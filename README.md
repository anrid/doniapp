# Doni

An app of sorts.

# Installation

```
# yarn > npm, so ..
brew update
brew install yarn
# or
curl -o- -L https://yarnpkg.com/install.sh | bash

# Run the app.
cd doniapp
yarn install
yarn start
```
